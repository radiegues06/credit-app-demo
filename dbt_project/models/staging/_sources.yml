version: 2

sources:
  - name: raw
    database: main
    schema: main
    tables:
      - name: raw_merchants
      - name: raw_transactions
      - name: raw_installments
      - name: raw_payments
      - name: raw_settlements
