ume_finops:
  target: dev
  outputs:
    dev:
      type: sqlite
      threads: 1
      database: 'database'
      schema: 'main'
      schemas_and_paths:
        main: '../database/ume_finops.db'
      schema_directory: '../database'
